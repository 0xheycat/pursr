interface:
  display_name: "Pursr"
  short_description: "Repo-specific patterns and workflows for pursr"
  default_prompt: "Use the pursr repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true