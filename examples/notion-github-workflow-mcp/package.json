{
  "name": "notion-github-workflow-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Notion-first GitHub workflow MCP gateway with repo health, CI, PR risk, changelog, and safe workflow tools.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
